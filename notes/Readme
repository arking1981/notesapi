1. Compile/deploy in the usual way with the Ant script: ant -Dwar.name=api deploy
2. The base URL is: http://localhost:8080/api/notes/.
3. Various curl scripts or the equivalent can be used to confirm that the get/post operations are supported.

